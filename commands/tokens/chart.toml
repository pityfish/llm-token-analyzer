description = "Generate and open interactive HTML visual reports."
prompt = """
请运行脚本生成可视化报告并简要告知我报告已生成并打开，同时提炼一下目前数据中的一个关键趋势。

脚本输出：
!{node $(find ~/.gemini/extensions -maxdepth 2 -type d -name "llm-token-analyzer" | head -n 1)/scripts/generate_report.cjs}
"""
