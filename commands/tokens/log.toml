description = "Manually log token usage for a specific session."
prompt = """
手动记录脚本执行结果：
!{node ${extensionPath}/scripts/log_token.cjs {{args}}}

请使用用户偏好的语言，根据上述输出，确认记录是否成功，并总结该条记录的 Token 消耗及估算成本。
"""
