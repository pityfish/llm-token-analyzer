description = "Automatically sync token usage data from the temporary directory."
prompt = """
脚本执行结果：
!{node ${extensionPath}/scripts/auto_sync.cjs}

请根据上述执行结果，使用用户偏好的语言简短地告诉我本次同步了多少新对话，主要涉及哪些模型。
"""
